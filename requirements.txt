openai==1.59.9
httpx==0.28.1
python-dotenv==1.0.1
tiktoken==0.7.0
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Original Streamlit + full RAG pipeline — not needed for the FastAPI/Next.js stack.
# Keep them so `streamlit run app.py` still works locally.
streamlit>=1.28.0
langchain==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0
chromadb==0.5.0
